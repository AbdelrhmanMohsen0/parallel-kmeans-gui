# Parallel K-Means Clustering Algorithm
An implementation of the K-means clustering algorithm including a sequential and parallel version with a GUI to test and visualize a 2D synthetic dataset
with some metrics and comparisons between the sequential and the parallel version.